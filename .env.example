# Quote API Environment Variables

# Email destination
TO_EMAIL=Leetshegoss@gmail.com

# SMTP credentials (example with Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Sender email (defaults to SMTP_USER if not set)
FROM_EMAIL=your-email@gmail.com

# API port
PORT=3000
